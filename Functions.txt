String s
    s.substring(a,b)
    s.charAt(i)
    s.indexOf("a")
    s.trim()
    s.isEmpty()
    s.toChararray()
    s.split(" +")
    s.concat()

Integer 
    Integer.toString(123)
    Integer.parseInt("123")

Arrays
    Arrays.sort(a)
    Collections.sort(a)
    List is a interface and arraylist,Hashset are implemented from list

Hashset
    h.add()
    h.contains
    h.remove

Hashmap
    h.put()
    h.containskey()
    h.remove(key)
    h.get(key)
    h.keysets()
    h.values()
    


